CLI tool to automate semantic version releases for Deno, JSR and npm projects. Bumps version
in deno.json / jsr.json / package.json (syncing package-lock.json), commits, tags, and pushes
to git remote. Also exports the pure bumpVersion / syncPackageLockVersion helpers for reuse.
